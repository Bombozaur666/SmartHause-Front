export class Producer {
    constructor(
        public id: number,
        public name: string, 
        public active: boolean,
        public website: string,
        public comment: string
) {}    
}